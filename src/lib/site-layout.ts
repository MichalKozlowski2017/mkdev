/**
 * Jedna kolumna publicznej strony: ta sama szerokość i **symetryczne**
 * odstępy poziome na wszystkich szerokościach (bez zmiany paddingu przy breakpointach).
 */
/** `min-w-0` — flex/grid nie sztywnieje na szerokość dzieci (unika poziomego scrolla i przesunięć między stronami). */
export const siteContentClassName = "mx-auto w-full min-w-0 max-w-6xl px-4";
